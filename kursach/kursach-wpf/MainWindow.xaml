<Window x:Class="kursach_wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quiz App" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="20" />
            <Setter Property="Width" Value="200" />
            <Setter Property="Height" Value="100" />
            <Setter Property="Margin" Value="10" />
            <Setter Property="Background" Value="#1ABC9C" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="20"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="#2C3E50">
        <TextBlock Text="Select a Quiz" FontSize="36" Foreground="White" VerticalAlignment="Top" Margin="20" />

        <UniformGrid x:Name="QuizGrid" Columns="2" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="50">
            <Button Content="Quiz 1" Foreground="Black" Click="QuizButton_Click" Tag="1" />
            <Button Content="Quiz 2" Foreground="Black" Click="QuizButton_Click" Tag="2" />
            <Button Content="Quiz 3" Foreground="Black" Click="QuizButton_Click" Tag="3" />
            <Button Content="Quiz 4" Foreground="Black" Click="QuizButton_Click" Tag="4" />
        </UniformGrid>
    </Grid>
</Window>
